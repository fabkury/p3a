# RUN-20260828-02 — baseline soak, ABORTED after 12 min (reporter gating too strict)

- Diag build cc351650, playset "Work mix", 13:59–14:11 local.
- Generation 12 (40 ms frames) produces at 80–137 ms per frame (decode 55–114 ms):
  chronic overrun, resync every ~0.5 s. A 137 ms frame exceeded the
  "EMA + one frame duration" anomaly rule and was UART-reported. Rule relaxed to
  produce_us >= 2x EMA (heavy artworks vary +-50 % frame to frame; a starved
  producer is far beyond that). No in-scope stalls in the 12 min.
