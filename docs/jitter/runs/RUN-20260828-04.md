# RUN-20260828-04 — baseline soak, 5 min, first genuine stalls captured (then stopped to add probes)

- Diag build f1134ce4, playset "Work mix", 14:16–14:22 local, 4016 frames.
- In-scope stalls: 12 (plus 6 warns). Overrun (out of scope): 408 frames.
- Signature: single frames whose **upscale** time balloons from ~14 ms to
  50–280 ms (decode unchanged at ~2 ms) → producer late → lateness 100–295 ms.
  40 producer anomalies (>= 3x generation median), all upscale-dominated, in
  bursts: t=50–64 s, 128–158 s, 197–220 s (roughly 15–30 s of trouble every
  ~70 s). Not tied to an artwork (gens 2, 3, 5, 7).
- Not explained by: downloads (lift 0.8x), my HTTP pulls (2 of 40), loader loads.
  `JTR|T` during stalls: core 0 ~100 % busy (download_mgr 200–300 ms per 0.6 s,
  no download in progress → LAI verify sweep / SD stat storms), and BOTH
  upscale workers (top on core 0, bottom on core 1) accrue 3x their normal run
  time, i.e. they run slowly rather than being preempted → shared-resource
  contention (PSRAM / L2 cache / SD DMA) is the leading explanation (H3/H4).
  A stall-time `uxTaskGetSystemState` took 31 ms instead of 3 ms in the same
  window: everything memory-bound on core 0 was ~10x slower.
- UART also showed `fs_atomic: Rename failed` + `sd_health: SD write failure 1/3`
  at t=71 s: the SD card is throwing errors under this load.
- Added for RUN-05: link-time wraps marking every `sdmmc_read/write_sectors`
  and `esp_flash_read/write/erase_region`, and a `verify` mark around
  `lai_verify_run_slice`; analyzer gained a producer-anomaly section with
  per-kind lift, and dedupes by seq.
