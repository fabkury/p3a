# RUN-20260828-08 — confirmation soak, cut short (4 min of data), to be re-run

- Started 17:04 on the fixed diag build; Fab wrapped up at ~17:25 and will
  restart the soak later. Captured 3534 frames / 248 s: 0 stalls, 0 warns.
- Defect to check first thing: the puller's last pull was #8 at 17:08:10 and
  nothing after (no error in puller.stdout/stderr) although the process was
  still alive at -Stop time. Suspect: a blocking `requests.get` without the
  device answering (timeout=60 should have fired) or the 30 s sleep loop
  stuck; add a watchdog line per loop iteration to `pull_frames.py` and
  re-check `pull_state.json` heartbeat before trusting a long soak.
