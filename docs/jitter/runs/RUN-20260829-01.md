# RUN-20260829-01 — fix-1-only soak, 38 min: residual stalls from event_bus cache saves

- Diag build (fix 1 = 065d6c90 aligned http_fetch/loader buffers), "Work mix",
  08:59–09:38 local, 29 544 frames.
- In-scope stalls: 5 (7.8/h), warns 4, overrun 5702 (out of scope).
  Lateness max 788 ms.
- Every stall: a burst of `sd_write` by task `event_bus` (channel-cache saves
  via fs_atomic from `psram_malloc` buffers: 15.5 KB in 94 ms = bounce pace)
  plus dozens of 512-byte FAT/directory writes at 1–35 ms each; the frame in
  flight sees its upscale go 16 → 118 / 232 / 779 ms.
- The card is visibly struggling: single 512-byte writes of 34–35 ms.
- Fix 2 (`5496ded0`, aligned psram_malloc) removes the bounce for the big
  writes but not the 512-byte metadata writes, and does not explain WHY an
  in-flight SD write slows both upscale halves 10–50x (H4). Decision: run the
  Phase 4 SD provocation (PSRAM-misaligned vs PSRAM-aligned vs internal
  buffers, 512 B vs 32 KB) before the next soak.
