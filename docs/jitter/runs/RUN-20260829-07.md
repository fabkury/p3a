# RUN-20260829-07 — fix 1 + fix 2 soak, 1.84 h: 5 stalls (2.7/h), all during channel-cache flush bursts

- Diag build 5496ded0 (+provoke variants), "Work mix", 10:11–12:02 local, 86 094 frames.
- In-scope stalls 5 (2.7/h; RUN-01 with fix 1 only: 7.8/h; RUN-06 unfixed: 37.7/h),
  warns 2, overrun 9918 (out of scope). Lateness max 302 ms.
- Attribution: every stall sits inside a burst of single-sector SD reads/writes
  by task `event_bus` (100–240 commands within 0.1–0.7 s). Those bursts are
  `channel_cache_flush_all()` saving several dirty caches back-to-back (temp
  file + rename + FAT/dir updates each). Of 16 bursts ≥ 30 commands in the run,
  the three ≥ 180 commands are the three stall times (6425 s, 6504 s, 6654 s);
  bursts of ~60 commands (one cache) never stalled. Not tied to artwork swaps
  (3 % of event_bus SD ops fall within 3 s of a swap).
- Both upscale halves slow down during the burst (top on core 0, bottom on
  core 1), so it is not core-0 scheduling; fix 3 (band stealing) cannot help
  here and did not remove the provoked bounce-loop stalls either.
- Fix 4: pace `channel_cache_flush_all` (300 ms gap between saves) so no burst
  exceeds single-cache size. A/B: RUN-10 (fixes 1+2+3) vs RUN-11 (+ fix 4).
