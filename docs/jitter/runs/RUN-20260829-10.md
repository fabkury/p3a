# RUN-20260829-10 — fixes 1+2+3 soak, 0.96 h: 3 stalls (3.1/h); fix 3 changes nothing here

- Diag build 79d637c0 (+ msync provoke), "Work mix", 12:22–13:20 local, 47 679 frames.
- In-scope stalls 3 (3.1/h), warns 1, overrun 9961. Max lateness 304 ms.
- #1 (47 min, 197 ms): inside a `download_mgr` burst of single-sector directory
  reads = LAI verify sweep (batches of 16 stat() ≈ 170–209 reads per ~100 ms;
  941 such bursts in the run, one stalled) → fix 5 (batches of 4).
- #2 (56 min, 143 ms): inside an `event_bus` cache-flush burst → fix 4.
- #3/#4 (63 min, 257 / 234 ms): NO SD activity in the window. #3 shows the
  consumer waiting 143 ms in the vsync loop; #4 shows decode 24 → 144 ms while
  the diag reporter was printing #3's report on UART (jtr_report 227 ms CPU in
  a 0.38 s window). Suspected observer effect / H5 (UART output on core 0
  slowing core 1) — tested next with the `log` provocation on the bar GIF.
- Fix 3 (band stealing) confirmed irrelevant to these classes; kept as hygiene.
