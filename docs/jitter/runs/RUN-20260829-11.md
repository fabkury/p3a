# RUN-20260829-11 — fixes 1–5 soak, 4.63 h: 2 in-scope stalls (0.43/h)

- Diag build 05c37fa5 (+ marked log provoke), "Work mix", 13:31–18:09 local, 220 440 frames.
- Analyzer: in-scope stalls 2 (0.43/h; ladder: 37.7 → 7.8 → 2.7 → 3.1 → 0.43),
  warns 1, overrun 28 103. UART reporter: 13 reports, of which one cluster of
  6 within 30 s at 17:11 and one cluster at 18:0x, both on zero-margin
  artworks (30 ms frames, ~28 ms produce) during `anim_loader` single-sector
  read storms; one 176 ms event on a failed artwork load (error logs on UART).
- Discovery from this run's marks: `anim_loader` issued 875 246 SD reads, all
  512 B, i.e. artwork file data read one sector per command through newlib's
  512-byte stdio buffer (`CONFIG_FATFS_VFS_FSTAT_BLKSIZE=0`). A 4.6 MB artwork
  = 9523 commands over 5.6 s. Fix 6 (POSIX `read()` in loader_service and
  pin_lists_copy, file-sized aligned stdio buffer for cache loads) verified on
  the device: 64 KB multi-sector reads at ~3.8 ms each, 50 loader reads per
  minute instead of ~5000.
