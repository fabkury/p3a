# RUN-20260829-12 — overnight soak, fixes 1–6, 12.07 h: only OTA-check stalls remained

- Diag build 76894598, "Work mix", 18:22–06:27 local, 561 875 frames, no reboots.
- In-scope stalls 7, ALL of one new class: `ota_check`. Two spontaneous at
  06:19 (the periodic update check: lateness 696 / 613 ms with the frame ready
  164 ms EARLY, i.e. the consumer was preempted) and five from three manual
  `POST /ota/check` calls made at 06:2x to confirm it (2 → 7 non-overrun
  stalls). Until 06:19 the night was clean: 0 stalls in 12 h, 64 119 overrun
  frames (out of scope), max lateness 319 ms (the resync sawtooth).
- Mechanism (H2, confirmed): `ota_check` is created at prio 3 but inherits
  prio 18 while it holds a socket/TLS lock that lwIP's tcpip task waits on;
  unpinned, it ran its TLS handshake on core 1 above the consumer (prio 6).
- Fix 7 (`79da6380`): lwIP tcpip task pinned to core 0 (release sdkconfig,
  `CONFIG_LWIP_TCPIP_TASK_AFFINITY_CPU0=y`) and the OTA check task created
  pinned to core 0. Verified on the device: three manual OTA checks → 0 stalls,
  max lateness 8 ms (unfixed: 5 stalls up to 696 ms).
- Fix 6 (multi-sector artwork reads) held: the loader read storms are gone.
