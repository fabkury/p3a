# RUN-20260830-06 — Phase 7 soak, release config + trace only: PASS (closed by Fab)

- Build: `build-trace` = release `sdkconfig` + `sdkconfig.trace.defaults`
  (trace on, 32768-entry ring, no FreeRTOS run-time stats, no dev endpoints),
  fixes 1–8 + Phase 6. Verified before start: `runtime_stats=false`,
  `dev_endpoints=false`, `__wrap_sdmmc_wait_for_idle` in the map.
- "Work mix", 14:09–17:13 local, 3.06 h, 146 901 valid frames, no reboots.
- **In-scope stalls 0, warns 0. Lateness p99 36.7 ms, max 79.8 ms.**
  No frame in the whole run reached even the 100 ms bar — max was 79.8 ms.
- Device-side counters showed 121 "warns": all producer-explained frames of
  overrun artworks (the firmware warn counter tallies before the overrun
  gate); the analyzer classifies every one out of scope. 0 UART stall reports.
- Fab reviewed the hourly heartbeats and closed the pass at the 3 h mark:
  "Close the phase 7 pass now, I am satisfied with it."
- Meaning: the pass bar is met on a build whose only delta from release is
  `CONFIG_P3A_FRAME_TRACE=y` — the fixes carry the result without the diag
  scheduler options. Phase 7 done; next is the final merge.
