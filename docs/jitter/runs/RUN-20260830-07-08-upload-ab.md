# RUN-20260830-07/08 — fix 9 A/B: upload stress phase, before vs after the 64 KB stdio buffer

Same build config (build-trace: release sdkconfig + trace only), same playset
("Work mix"), same procedure (`stress.py --phases upload`: 3 x 30 fps bar GIF
upload, 15 s apart). Before = pre-fix-9 binary (the RUN-06 soak binary);
after = fix 9 (`6dae17cc`: /upload writes through a 64 KB cache-line-aligned
PSRAM stdio buffer instead of newlib's 512 B default).

| | frames | upscale med / max ms | producer anomalies | stalls / warns | max lateness in window |
|---|---|---|---|---|---|
| before (RUN-07) | 2432 | 16.5 / 19 | 11 | 0 / 0 | 79 ms |
| after (RUN-08) | 2320 | 16.5 / 21 | **2** | 0 / 0 | **30 ms** |

- Fix 8 already removed the stall risk from uploads (0 stalls both sides; on
  the diag build back on 2026-08-30 morning the same phase showed upscale max
  93 ms and 4 warns). Fix 9 removes most of the residual drag: 11 -> 2
  anomalous production times, window max lateness 79 -> 30 ms.
- Verdict: fix 9 kept. It rides to main with the final merge.
