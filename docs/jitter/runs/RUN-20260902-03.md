# RUN-20260902-03 — 3.3 h soak on Espressif's #19034 patch (wrapper off): PASS

- Build: `build-diag-patch` 14f03b740e1c = release `sdkconfig` +
  `sdkconfig.diag.defaults` + `sdkconfig.nowrap.defaults`, IDF v5.5.4 tree
  73550728 with `sdmmc_back_off_between_CMD13_polls_v5.5.4.patch` applied.
  Device confirmed `sd_idle_wait_wrap=false, idf_sdmmc_backoff_patch=true`.
- "Work mix" (8 channels), 11:53–15:13 local, **3.30 h covered, 0 coverage
  gaps**, 160 760 frames (160 364 valid). First ~2 min on the bar GIF left by
  the reproducer's upload; Work mix re-activated at 11:55.
- **In-scope stalls 0, warns 1. Lateness p99 39.1 ms, max 77.2 ms.**
  PASS BAR met. 434 overrun frames (producer-bound artworks, out of scope),
  0 producer anomalies in 160 364 frames.
- Reference on our wrapper (RUN-20260830-06, same playset, trace-only build):
  0 stalls, 0 warns, p99 36.7 ms, max 79.8 ms. Same picture.
- **5 epochs (4 reboots)**, all external: Fab was handling the fragile USB-C
  cable at 12:01–12:09 and 13:54 (confirmed). Signature: logger
  "GetOverlappedResult failed (Access is denied)" (CH343 re-enumerated),
  puller epoch, ROM `rst:` line lost. `run_audit.py` classifies all four as
  "external (USB link dropped)". No panic, no watchdog, no deliberate reboot.
  `analyze.py` now merges the epoch files (it used to read epoch 0 only).
- SD write spans (`sd_write` marks, ms): n 25 351, median 6.6, **p90 52.5,
  p99 57.5**, max 347. Wrapper reference (RUN-06): n 36 422, median 7.3,
  p90 37.2, p99 46.8, max 360. The patch's 32 ms cap shows up in the tail: a
  write whose busy period ends between 25 and 57 ms is only noticed at
  57.5 ms, so the slow-card 32 KB download writes moved from ~37–47 ms to
  ~52–58 ms. Total time inside SD writes 442 s vs 598 s, but this run also
  did fewer writes per frame (136 vs 247 per 1000 frames: caches fuller than
  on 2026-08-30), so the totals are not comparable; the percentiles are.
- Background noise, pre-existing and unrelated to the arm: 69 `fs_atomic`
  "Rename failed (errno=2)" lines (RUN-05/06/03 of 2026-08-30 had 34–48);
  SD health latch not tripped.
- Verdict: on the stall question the patch is as good as the wrapper over a
  3 h normal workload. Its cost is write-completion latency on long busy
  periods (the doubling overshoot), not stalls.
